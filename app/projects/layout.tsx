export default function ProjectsLayout({ children }: {
    children: React.ReactNode;
  }) {
    return (
      <div>
        <p>Projects layout goes here</p>
        {children}
      </div>
    );
  }